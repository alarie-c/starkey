from file import file;

class Data {
    file: File,
    content: str,
}

methods Data {
    def example(x: int): str {
        const x = 5 + 5;
        return x;
    }

    def hello(y: str, a: int) {
        var x = 10 + 5 * example.hello;
        return x;
    }
}